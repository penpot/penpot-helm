{{- if and (eq (include "penpot.adminConsoleEnabled" .) "true") .Values.adminConsole.autoscaling.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "penpot.fullname" . }}-admin-console
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "penpot.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "penpot.fullname" . }}-admin-console
  minReplicas: {{ .Values.adminConsole.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.adminConsole.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- toYaml .Values.adminConsole.autoscaling.hpa.metrics | nindent 4 }}
{{- end }}
