{{- if and (eq (include "penpot.adminConsoleEnabled" .) "true") .Values.adminConsole.autoscaling.vpa.enabled }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "penpot.fullname" . }}-admin-console
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "penpot.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "penpot.fullname" . }}-admin-console
  updatePolicy:
    updateMode: {{ .Values.adminConsole.autoscaling.vpa.updateMode | quote }}
  {{- with .Values.adminConsole.autoscaling.vpa.resourcePolicy }}
  resourcePolicy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
