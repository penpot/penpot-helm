# The MCP server scales like any other component.
mcp:
  replicaCount: 3
  pdb:
    enabled: true
    minAvailable: 1
  autoscaling:
    hpa:
      enabled: true
      minReplicas: 2
      maxReplicas: 6
