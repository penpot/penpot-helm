---
apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: penpot
  labels:
    app: valkey
spec:
  type: ClusterIP
  selector:
    app: valkey
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
      protocol: TCP
